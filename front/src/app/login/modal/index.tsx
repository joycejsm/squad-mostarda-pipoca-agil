// import { Content, Modal, ModalHeader, Card, Price, Description, Colors, Sizes, StyledButton, SoldBy, Info } from "./styles";

export default function Modal(props) {

  return (
    <dialog open >
      <p>Modal - "Esqueci a senha"</p>
    </dialog>
  );
}